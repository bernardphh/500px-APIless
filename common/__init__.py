#python 3.6

# This empty __init__.py file allows the containing folder (common) to be treated as a package.
# So that we can import the modules using [folder name].[module name]
# example: import common/base.py
